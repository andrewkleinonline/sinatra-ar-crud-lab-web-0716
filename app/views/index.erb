<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>All your posts:</h1>
    <ul>
      <% @posts.each_with_index do |post,index| %>
      <li><%= index+1 %>. <%= post.name %></li>
      <%= post.content %>
      <% end %>
    </ul>

  </body>
</html>
